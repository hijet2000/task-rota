import React from 'react';

// This is a placeholder component to resolve a build error from an empty file.
// The main PWA shell component is located at /components/PwaShell.tsx
const PwaShellPlaceholder: React.FC = () => {
    return (
        <div>
            PWA Shell Placeholder.
        </div>
    );
};

export default PwaShellPlaceholder;
